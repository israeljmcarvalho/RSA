Privatekey <- rsa_keygen(512)
ublicKey <- Privatekey$pubkey
m <- bignum(charToRaw("Habib, arruma minhas notas no Teams"))
class (m)
print(m)
e <- PublicKey$data$e
Privatekey <- rsa_keygen(512)
library(openssl)
rm(list = ls())
(key <- rsa_keygen(512))
rm(list = ls())
key <- rsa_keygen(512)
pubkey <- key$pubkey
text <- charToRaw("Habib, arruma minhas notas no Teams")
class (text)
ciphertext <- rsa_encrypt(text, pubkey)
rawToChar(rsa_decrypt(ciphertext, key))
rm(list = ls())
Privatekey <- rsa_keygen(512)
ublicKey <- Privatekey$pubkey
PublicKey <- Privatekey$pubkey
rm(list = ls())
Privatekey <- rsa_keygen(512)
PublicKey <- Privatekey$pubkey
m <- bignum(charToRaw("Habib, arruma minhas notas no Teams"))
class (m)
print(m)
rm(list = ls())
Privatekey <- rsa_keygen(512)
PublicKey <- Privatekey$pubkey
text <- bignum(charToRaw("Habib, arruma minhas notas no Teams"))
class (text)
print(text)
print(m)
rm(list = ls())
Privatekey <- rsa_keygen(512)
PublicKey <- Privatekey$pubkey
text <- bignum(charToRaw("Habib, arruma minhas notas no Teams"))
class (text)
print(text)
# A chave pública: o par (n,e)
e <- PublicKey$data$e
n <- PublicKey$data$n
c <- (m ^ e) %% n # https://pt.wikipedia.org/wiki/RSA_(sistema_criptogr%C3%A1fico)
c <- (text ^ e) %% n # https://pt.wikipedia.org/wiki/RSA_(sistema_criptogr%C3%A1fico)
print(c)
text
c <- (text ^ e) %% n # https://pt.wikipedia.org/wiki/RSA_(sistema_criptogr%C3%A1fico)
c
text
c
rm(list = ls())
Privatekey <- rsa_keygen(512)
PublicKey <- Privatekey$pubkey
Msg <- bignum(charToRaw("Habib, arruma minhas notas no Teams"))
class (Msg)
print(Msg)
# A chave pública: o par (n,e)
e <- PublicKey$data$e
n <- PublicKey$data$n
Msg
MsgCripto <- (Msg ^ e) %% n # https://pt.wikipedia.org/wiki/RSA_(sistema_criptogr%C3%A1fico)
MsgCripto
MsgCripto
# Mensagem original em formato bigint ja criptografada
MsgCripto
# Mensagem original em formato bigint ainda não criptografada
Msg
# Mensagem original em formato bigint ainda não criptografada
Msg
# Mensagem original em formato bigint ja criptografada
MsgCripto
print(c)
base64_encode(MsgCripto)
d <- Privatekey$data$d
class(d)
out <- bignum_mod_exp(MsgCripto, d, n)
rawToChar(out)
rm(list = ls())
Privatekey <- rsa_keygen(512)
PublicKey <- Privatekey$pubkey
Msg <- bignum(charToRaw("Habib, arruma minhas notas no Teams"))
class (Msg)
print(Msg)
# A chave pública: o par (n,e)
e <- PublicKey$data$e
n <- PublicKey$data$n
# Mensagem original em formato bigint ainda não criptografada
Msg
MsgCripto <- (Msg ^ e) %% n # https://pt.wikipedia.org/wiki/RSA_(sistema_criptogr%C3%A1fico)
# Mensagem original em formato bigint ja criptografada
MsgCripto
base64_encode(MsgCripto)
d <- Privatekey$data$d
class(d)
out <- bignum_mod_exp(MsgCripto, d, n)
rawToChar(out)
rm(list = ls())
Privatekey <- rsa_keygen(512)
PublicKey <- Privatekey$pubkey
Msg <- bignum(charToRaw("Habib, arruma minhas notas no Teams"))
class (Msg)
print(Msg)
# A chave pública: o par (n,e)
e <- PublicKey$data$e
n <- PublicKey$data$n
# Mensagem original em formato bigint ainda não criptografada
Msg
MsgCripto <- (Msg ^ e) %% n # https://pt.wikipedia.org/wiki/RSA_(sistema_criptogr%C3%A1fico)
# Mensagem original em formato bigint ja criptografada
MsgCripto
base64_encode(MsgCripto)
d <- Privatekey$data$d
class(d)
out <- bignum_mod_exp(MsgCripto, d, n)
out
rawToChar(out)
out
Privatekey <- rsa_keygen(512)
PublicKey <- Privatekey$pubkey
Msg <- bignum(charToRaw("Habib, arruma minhas notas no Teams"))
class (Msg)
print(Msg)
# A chave pública: o par (n,e)
e <- PublicKey$data$e
n <- PublicKey$data$n
# Mensagem original em formato bigint ainda não criptografada
Msg
MsgCripto <- (Msg ^ e) %% n # https://pt.wikipedia.org/wiki/RSA_(sistema_criptogr%C3%A1fico)
# Mensagem original em formato bigint ja criptografada
MsgCripto
base64_encode(MsgCripto)
d <- Privatekey$data$d
out <- bignum_mod_exp(MsgCripto, d, n)
out
class(out)
rawToChar(out)
class(out)
rawToChar(out)
rm(list = ls())
Privatekey <- rsa_keygen(512)
PublicKey <- Privatekey$pubkey
Msg <- bignum(charToRaw("Habib, arruma minhas notas no Teams"))
class (Msg)
print(Msg)
# A chave pública: o par (n,e)
e <- PublicKey$data$e
n <- PublicKey$data$n
# Mensagem original em formato bigint ainda não criptografada
Msg
MsgCripto <- (Msg ^ e) %% n # https://pt.wikipedia.org/wiki/RSA_(sistema_criptogr%C3%A1fico)
# Mensagem original em formato bigint ja criptografada
MsgCripto
base64_encode(MsgCripto)
d <- Privatekey$data$d
out <- bignum_mod_exp(MsgCripto, d, n)
out
class(out)
rawToChar(out)
rm(list = ls())
#	Cria chave privada
privateKey <- rsa_keygen(512)
class (privateKey)
#	Cria Chave públic
publicKey <- privateKey$pubkey
class (publicKey)
text <-("Habib, arruma minhas notas no Teams")
class (text)
# charToRaw
# converte para um vetor onde cada byte e representado separadamente como um par de dígitos hexadecimais
text <- charToRaw(text)
class (text)
text
# Criptografa a chave temporária usando a chave RSA pública
ciphertext <- rsa_encrypt(text, publicKey)
class (ciphertext)
ciphertext
# Descriptografa o objeto anterior
x <- rsa_decrypt(ciphertext, privateKey)
# converte objeto ja descriptografado de Raw para Character
rawToChar(x)
rm(list = ls())
key <- rsa_keygen(512)
pubkey <- key$pubkey
text <- charToRaw("Habib, arruma minhas notas no Teams")
class (text)
ciphertext <- rsa_encrypt(text, pubkey)
rawToChar(rsa_decrypt(ciphertext, key))
rm(list = ls())
Privatekey <- rsa_keygen(512)
PublicKey <- Privatekey$pubkey
Msg <- bignum(charToRaw("Habib, arruma minhas notas no Teams"))
class (Msg)
print(Msg)
# A chave pública: o par (n,e)
e <- PublicKey$data$e
n <- PublicKey$data$n
# Mensagem original em formato bigint ainda não criptografada
Msg
MsgCripto <- (Msg ^ e) %% n # https://pt.wikipedia.org/wiki/RSA_(sistema_criptogr%C3%A1fico)
# Mensagem original em formato bigint ja criptografada
MsgCripto
base64_encode(MsgCripto)
d <- Privatekey$data$d
out <- bignum_mod_exp(MsgCripto, d, n)
out
class(out)
rawToChar(out)
#	Cria chave privada
privateKey <- rsa_keygen(512)
class (privateKey)
#	Cria Chave públic
publicKey <- privateKey$pubkey
class (publicKey)
text <-("Habib, arruma minhas notas no Teams")
class (text)
# charToRaw
# converte para um vetor onde cada byte e representado separadamente como um par de dígitos hexadecimais
text <- charToRaw(text)
class (text)
text
# Criptografa a chave temporária usando a chave RSA pública
ciphertext <- rsa_encrypt(text, publicKey)
class (ciphertext)
ciphertext
# Descriptografa o objeto anterior
x <- rsa_decrypt(ciphertext, privateKey)
# converte objeto ja descriptografado de Raw para Character
rawToChar(x)
